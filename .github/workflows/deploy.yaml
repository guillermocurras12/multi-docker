name: Deploy MultiDocker
on:
  push:
    branches:
      - master # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t gcurrasazt/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true gcurrasazt/react-test npm test

      - run: docker build -t gcurrasazt/multi-client ./client
      - run: docker build -t gcurrasazt/multi-nginx ./nginx
      - run: docker build -t gcurrasazt/multi-server ./server
      - run: docker build -t gcurrasazt/multi-worker ./worker

      - run: docker push gcurrasazt/multi-client
      - run: docker push gcurrasazt/multi-nginx
      - run: docker push gcurrasazt/multi-server
      - run: docker push gcurrasazt/multi-worker
